document.addEventListener('DOMContentLoaded',()=>{const host=document.getElementById('gaccaCalendar');if(!host)return;let view=new Date();view=new Date(view.getFullYear(),view.getMonth(),1);function draw(){const y=view.getFullYear(),m=view.getMonth();const name=view.toLocaleString('en-US',{month:'long',year:'numeric'});const first=new Date(y,m,1).getDay();const days=new Date(y,m+1,0).getDate();let cells='';for(let i=0;i<first;i++)cells+='<div class="cal-day cal-muted"></div>';for(let d=1;d<=days;d++)cells+=`<div class="cal-day"><strong>${d}</strong><div class="cal-events" data-date="${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}"></div></div>`;host.innerHTML=`<div class="cal-head"><button id="calPrev" aria-label="Previous month">&#8249;</button><h3>${name}</h3><button id="calNext" aria-label="Next month">&#8250;</button></div><div class="cal-week"><span>Sun</span><span>Mon</span><span>Tue</span><span>Wed</span><span>Thu</span><span>Fri</span><span>Sat</span></div><div class="cal-grid">${cells}</div><p class="note">Scheduled GACCA events will appear on their dates as they are published.</p>`;document.getElementById('calPrev').onclick=()=>{view=new Date(y,m-1,1);draw()};document.getElementById('calNext').onclick=()=>{view=new Date(y,m+1,1);draw()};}draw();});